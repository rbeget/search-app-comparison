# Competitor App Comparison — Radar

Public GitHub Pages site for an interactive radar chart comparing Google App, Chrome, Edge, ChatGPT, Copilot, Gemini, and Perplexity across 10 product dimensions.

## 🚀 How this is hosted
This repo is designed for **GitHub Pages (Project Site)**. The page will be available at:

```
https://rbeget.github.io/competitor-app-comparison/
```

## ✅ Quick publish steps
1. Create a repo named **`competitor-app-comparison`** under your `rbeget` account.
2. Upload these files: `index.html`, `.nojekyll`, and `README.md`.
3. In the repo, go to **Settings → Pages**.
4. Under **Build and deployment**, set **Source = Deploy from a branch**.
5. Choose **Branch = main**, **Folder = /(root)**, then click **Save**.
6. Wait ~30 seconds, then open the URL above.

## 🛠 Local preview (optional)
Just open `index.html` in your browser. Everything is inline (uses Plotly CDN).

## ✍️ Edits
- Update the data inside `index.html` (`series` object) to change scores.
- Styling is pure CSS at the top of `index.html`.
